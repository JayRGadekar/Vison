<!-- Small PRs get looked at faster. If this is a big one, an issue first
     saves us both writing the same thing twice. -->

## What this changes

## Why

## How it was tested

<!-- Tick what applies. Not every change needs all of them. -->

- [ ] `cd app && npm test`
- [ ] Built and ran it (`cmake --build build --config Release` + `npm run build`)
- [ ] Generated something end to end
- [ ] GPU it was tested on:

<!-- If you changed anything under third_party/, regenerate the patch file so a
     fresh clone reproduces your tree, and check it applies both ways. See
     CONTRIBUTING.md and PATCHES.md. -->
