add_library(pipeline_image_upscaling STATIC src/image_upscale.cpp)
target_include_directories(pipeline_image_upscaling PUBLIC include ../common/include)
# stb supplies the JPEG encoder; visp::image_save can only write PNG.
target_link_libraries(pipeline_image_upscaling PRIVATE vison_core visioncpp stb)
